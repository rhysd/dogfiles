[user]
	name = rhysd
	email = lin90162@yahoo.co.jp
[core]
	editor = vim
	excludesfile = ~/.global.gitignore
[color]
	ui = true
[alias]
	st  = status -sb
	bl  = blame
	br  = branch
	cm  = commit
	cmv = commit -v
	cmm = commit -m
	cma = commit --amend
	cl  = clone
	di  = diff
	dic = diff --cached
	fe  = fetch
	lo  = log --graph --pretty=format:'%C(Yellow)%h %C(Cyan)%ad %C(White)%s%C(Green)%d %C(Red)[%an]' --date=short
	mg  = merge
	ps  = push
	pl  = pull
	plr = pull --rebase
	rb  = rebase
	rbi = rebase -i
	rbc = rebase --continue
	re  = remote
	ad  = add
	adp = add -p
	co  = checkout
	cob = checkout -b
	sh  = show
	lsf = ls-files
	lst = ls-tree
	he  = help
	gr  = grep
	lop = log -p
	bg  = background
	bps = background push
	bpl = background pull
	bplr = background pull --rebase
	bcl = background clone
[push]
	default = simple
